[project]
name = "claude-ws"
version = "0.1.0"
description = "General-purpose workspace for Claude Code ad-hoc tasks"
requires-python = ">=3.10"
dependencies = [
    "easyocr>=1.7.2",
    "pillow>=12.1.1",
    "python-pptx>=1.0.2",
]
