# text 内下划线修复候选

## F: _ 移到 \text 外

$H = (\text{asset}, \text{signal}\_\text{source}, \text{reject}\_\text{when})$ 结束。

## G: 用 \text{signal\textunderscore source}

$H = (\text{signal\textunderscore source})$ 结束。

## H: 整体不用公式，改行内代码

研究结论six要素：`H = (asset, window, signal_source, benchmark, metric, reject_when)` 结束。

## I: \text 内空格代替下划线

$H = (\text{asset}, \text{signal source}, \text{reject when})$ 结束。
