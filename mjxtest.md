# PSR/DSR GitHub 渲染测试

## 变体 A：原样（div align + $$ + aligned）

<div align="center">

$$
\begin{aligned}
PSR(SR^{*}) & = \Phi \left( \frac{(\hat{SR} - SR^{*})\sqrt{T-1}}{\sqrt{1 - \gamma_{3}\hat{SR} + \frac{\gamma_{4}-1}{4}\hat{SR}^{2}}} \right) , \\
DSR & = PSR(SR^{*}_{multipletrials}).
\end{aligned}
$$

</div>

## 变体 B：纯 $$，无 div

$$
\begin{aligned}
PSR(SR^{*}) & = \Phi \left( \frac{(\hat{SR} - SR^{*})\sqrt{T-1}}{\sqrt{1 - \gamma_{3}\hat{SR} + \frac{\gamma_{4}-1}{4}\hat{SR}^{2}}} \right) , \\
DSR & = PSR(SR^{*}_{multipletrials}).
\end{aligned}
$$

## 变体 C：div 后不空行

<div align="center">

$$
PSR(SR^{*}) = \Phi(x)
$$

</div>

## 变体 D：单行 $$ 无 aligned

$$PSR(SR^{*}_{multipletrials})$$
